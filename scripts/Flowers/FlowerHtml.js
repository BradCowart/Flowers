const Flower = (flower) => {
    return `
        <section class="flowerclass">
            <header>
                <h2>${flower.name}</h2>
            </header>
        </section>
    `
}

export default Flower