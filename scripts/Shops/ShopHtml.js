const Shop = (shopObject) => {
    return `
        <section class="shopclass">
            <header>
                <h2>${shopObject.name}</h2>
            </header>
        </section>
    `
}

export default Shop